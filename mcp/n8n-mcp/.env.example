# n8n-MCP 환경 변수 설정
# 이 파일을 .env로 복사한 뒤, 자신의 값으로 바꿔주세요.
# cp .env.example .env

# MCP 서버 모드 (stdio = 로컬 사용, http = 원격 사용)
MCP_MODE=stdio

# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=error

# n8n 서버 주소 (보통 바꿀 필요 없어요)
N8N_API_URL=http://localhost:5678

# n8n API 키 (n8n 설정 > API에서 발급)
N8N_API_KEY=여기에_n8n_API_키를_넣으세요
